# Environment Variables for Personal Activity Tracker

# Database Configuration
# For local development, leave MONGODB_URI empty to use SQLite
# For production, set MONGODB_URI to your MongoDB Atlas connection string
MONGODB_URI=

# Application Configuration
NODE_ENV=development
PORT=3000

# For production deployment, you'll need:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/activity_tracker?retryWrites=true&w=majority
